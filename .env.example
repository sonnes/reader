# Base path for deployment (e.g., /reader/ for GitHub Pages)
# Default: /
VITE_BASE_PATH=/

# App URL for production (used in RSS feeds, SEO, etc.)
VITE_APP_URL=https://readerapp.in

# CORS proxy URL for fetching feeds (leave empty to use fallback proxy)
VITE_CORS_PROXY_URL=https://readerapp.in/api/proxy?url=
